<div class=“container”>
 <%= simple_form_for [@cocktail, @dose] do |f| %>
   <%= f.input :ingredient_id, collection: Ingredient.all %>
   <%= f.input :description %>
   <%= f.submit "Drink It Up", class: "btn btn-danger" %>
 <% end %>
</div>
